import {createSlice} from "@reduxjs/toolkit";

const initialState = {}

const slice = createSlice({
    name: 'bestChoice',
    initialState,
    reducers: {

    }
})

export const bestChoiceReducer = slice.reducer